#include <vector>
#include <iostream>
void heapDynamicArray() {
    std::vector<int> arr;
    for(int i = 0; i < 5; ++i) {
        arr.push_back(i * 2);
    }
    for(int num : arr) {
        std::cout << num << " ";
    }
}
