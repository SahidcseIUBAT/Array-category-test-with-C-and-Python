#include <iostream>
void fixedDynamicArray() {
    int arr[5]; // fixed size, declared in function
    for(int i = 0; i < 5; ++i) {
        arr[i] = i * 2;
    }
    for(int i = 0; i < 5; ++i) {
        std::cout << arr[i] << " ";
    }
}
